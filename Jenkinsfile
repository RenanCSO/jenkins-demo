pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'python --version'
                echo 'Hello world from Jenkins'
            }
        }
    }
}
